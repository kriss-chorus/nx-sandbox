apiVersion: v1
kind: Secret
metadata:
  name: github-dashboard-secrets
  namespace: github-dashboard
type: Opaque
data:
  # Base64 encoded values - replace with your actual values
  # To encode: echo -n "your_github_token" | base64
  GITHUB_API_TOKEN: ""  # Add your GitHub token here (base64 encoded)

