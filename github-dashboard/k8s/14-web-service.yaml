apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: github-dashboard
  labels:
    app: github-dashboard-web
spec:
  selector:
    app: github-dashboard-web
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: NodePort
