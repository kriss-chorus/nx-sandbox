apiVersion: v1
kind: ConfigMap
metadata:
  name: github-dashboard-config
  namespace: github-dashboard
data:
  NODE_ENV: "development"
  PORT: "3001"
  DATABASE_URL: "postgresql://postgres:password@postgres-service:5432/github_dashboard"
  GITHUB_API_BASE_URL: "https://api.github.com"

