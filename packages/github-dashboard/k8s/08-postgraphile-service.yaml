apiVersion: v1
kind: Service
metadata:
  name: postgraphile-service
  namespace: github-dashboard
  labels:
    app: postgraphile
spec:
  selector:
    app: postgraphile
  ports:
  - port: 5001
    targetPort: 5001
    protocol: TCP
  type: ClusterIP
