apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: github-dashboard
  labels:
    app: github-dashboard-api
spec:
  selector:
    app: github-dashboard-api
  ports:
  - port: 80
    targetPort: 3001
    protocol: TCP
  type: ClusterIP
